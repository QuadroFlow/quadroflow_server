server:
  port: 8080
  host: 127.0.0.1

env:
  mode: debug

openapi:
  enable: true
  version: 0.1.0
  title: QuadroFlow API
  description: API automatically generated by Vaden.

storage:
  provider: local
  local:
    folder: "./public"
